<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LOGIN</title>
    <link rel="icon" href="images/KARE_EDUSYNC_LOGO.png" type="image/png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(rgb(255, 208, 208), rgb(143, 231, 251));
            font-family: Arial, sans-serif;
        }

        .form {
            width: 320px;
            margin: 0 auto;
            padding: 40px 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease;
        }

        .form:hover {
            transform: translateY(-5px);
        }

        .form img {
            display: block;
            margin: 0 auto 20px;
            max-width: 100%;
            height: auto;
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            font-weight: 600;
            font-size: 24px;
            color: #333;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        .form-groups {
            margin-bottom: 20px;
        }

        .form-groups label {
            font-weight: bold;
            margin-bottom: 5px;
            color: #333;
        }

        .form-groups input {
            padding: 10px;
            font-size: 16px;
            border: 1px solid #ccc;
            border-radius: 5px;
            transition: border-color 0.3s ease;
        }

        .form-groups input:focus {
            outline: none;
            border-color: dodgerblue;
        }

        .submit-btn {
            padding: 12px;
            background-color: dodgerblue;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .submit-btn:hover {
            background-color: #0056b3;
        }

        p {
            margin-top: 10px;
            font-size: 14px;
            text-align: center;
        }

        .sign-btn {
            display: inline-block;
            padding: 5px 10px;
            background-color: rgb(155, 178, 255);
            color: #333;
            cursor: pointer;
            border: none;
            border-radius: 3px;
            text-decoration: none;
            transition: background-color 0.3s ease;
        }

        .sign-btn:hover {
            background-color: rgb(110, 138, 255);
        }

        .social-icons {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .social-icons a {
            display: inline-block;
            margin: 0 10px;
            color: #333;
            font-size: 20px;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .social-icons a:hover {
            color: dodgerblue;
        }
    </style>
</head>

<body>
    <div class="form">
        <img src="images/KARE_EDUSYNC.png" alt="KARE EDUSYNC Logo" height="150px" width="340px">
        <h2>LOGIN</h2>
        <form action="/login" method="post" onsubmit="emitSocketId(event)">
            <div class="form-groups">
                <label for="userPhoneNumber">PHONE NUMBER:</label>
                <input type="text" id="userPhoneNumber" name="username" placeholder="Enter your phone number" required
                    autocomplete="on">
            </div>
            <div class="form-groups">
                <label for="password">PASSWORD:</label>
                <input type="password" id="password" name="password" placeholder="Enter your password" required
                    autocomplete="off">
            </div>
            <button type="submit" class="submit-btn">LOGIN</button>
            <p>Don't have an account? <a href="/signup" class="sign-btn">SignUp</a></p>
        </form>
        <div class="social-icons">
            <a href="#"><i class="fab fa-facebook-f"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-google"></i></a>
        </div>
    </div>
</body>

</html>
